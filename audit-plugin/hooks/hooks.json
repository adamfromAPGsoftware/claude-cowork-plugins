{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(cat | jq -r '.tool_input.file_path // .tool_input.filePath // empty'); if [ -n \"$FILE\" ] && echo \"$FILE\" | grep -q 'audit-data.json$'; then echo 'audit-data.json modified — run /apg-audit:build-deliverables to update HTML outputs'; fi"
          }
        ]
      }
    ]
  }
}
