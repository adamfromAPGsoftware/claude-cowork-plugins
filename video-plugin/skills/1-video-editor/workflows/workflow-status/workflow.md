# Workflow Status

**Status:** Stub — to be built via create-workflow

## Purpose

Check status of in-progress video content across the pipeline. Show which stage each video is at and surface any issues.

## Key Requirements

- List all in-progress videos and their current pipeline stage
- Show completion status per stage (ingest, analysis, clipping, editing, extraction)
- Surface any blocked or failed stages
- Report on active project context
